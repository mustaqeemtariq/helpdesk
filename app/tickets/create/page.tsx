import TicketForm from '@/tickets/create/ticket-form'

const CreateTicket = () => {
	return (
		<main>
			<h2 className="text-primary text-center">Add a New Ticket</h2>
			<TicketForm />
		</main>
	)
}

export default CreateTicket
